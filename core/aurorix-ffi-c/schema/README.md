# Public FFI Schema

This directory contains the public bootstrap transport schema only. It proves
the versioned envelope and bounded extension projection needed before a native
ABI exists. It is intentionally not the complete Aurorix facade.

The full reviewed schema, when approved for publication, must be supplied to
the generation script as an explicit local input. Private planning material
must not be copied into this directory, generated output, commits, or CI logs.
