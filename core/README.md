# Rust Core

The Rust workspace will contain coarse-grained crates for model, application,
storage, library, playback, audio, synchronization, extensions, and FFI. The
public architecture overview describes the published boundaries; the crates are
placeholders until implementation begins.
