# Aurorix Agent Entry Point

This public repository treats all tracked content as public. Read
`AGENTS/README.md` before working in the repository.

Do not commit Agent task state, private planning material, credentials,
production configuration, or operational details. Local Agent state belongs in the ignored `AGENTS/state/` directory.
