<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Aurorix.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Aurorix.Windows"
    xmlns:pages="using:Aurorix.Windows.Home"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Aurorix.Windows">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- The title bar is deliberately a small, named surface so the native
             window controls can remain owned by Windows while the page owns the rest. -->
        <Grid
            x:Name="AppTitleBar"
            Grid.Row="0"
            Height="32"
            Background="Transparent"
            Padding="18,0,0,0">
            <TextBlock
                VerticalAlignment="Center"
                FontSize="12"
                FontWeight="SemiBold"
                Foreground="#6B737A"
                Text="Aurorix Music  ·  流光" />
        </Grid>

        <pages:HomePage Grid.Row="1" />
    </Grid>
</Window>
